計算量的安全性について勉強している。

現在は意味的安全性と計算量的識別不可能性の等価性の部分をやっている。
テキストとして安永先生の本を見ており、関連するところのPDFを添付したので見てほしい。

これらをLean4で形式化して証明することで理解を深めている。またそれらはGithubで公開している。このチャットでは形式化と証明をクロード君に手伝ってもらうのが目的だよ。
MCPとしてfilesystemとlean-lsp-mcpが使える。また２つのレポジトリが見えるはずだ。１つは以前にやった完全秘匿性関連で読み取り専用、もう一つは計算量的安全性のレポジトリで読み書きOKだよ。ちゃんとアクセスできるか確認してほしい。ツールを使う際の注意事項は以下のとおり：

調べ物としてはどんどん使ってほしい。lean_local_searchなどをぜひ有効活用して、はるしネーションしないようにしてほしい。一方runの方はやる前に確認してほしい。こちらで証明を進めた方が早い場合もあるし、そもそも君にもよく考えて数学を組み立ててほしいんだ。
lean4の証明を通して、とお願いしたらrunを使ってほしい。その場合にも３回くらい失敗したら相談してほしい。
それと前のチャットでたぶんrunで証明が通らず証明を書き直してrunするループが止まらなくなり相当な量のコンテキストを消費したっぽい。勿体無いし、こちらでやるとsimp?などを使って進められることもあるので、途中で諦めて相談してほしい。


コーディングルールは：コメントは定理、定義、補題には必ず英語でつけること。途中でも重要な部分にはコメントをつけること。


見てわかるようにLean 4 + Mathlib 4だ。また確率はPMFを使っている。当面はこの道具立てで進めたい。

PDFに戻って、今やっているのは定理3.1の証明だよ。そのためのファイルがSemSec_implies_Ind.leanだよ。ここで意味的安全性がある精度で成り立てば、識別不可能性が成り立つことの証明を作成している。必要な定義はDefs.leanにある。また推測補題はGuessingLemma.leanに証明してある。その辺で汎用的に使えそうな道具はProbabilityUtils.leanにある。確認してほしい。

前のスレッドで推測補題を少し拡張したんだ。その時に最後に作ってもらった引き継ぎメモだよ。よく読んでファイルと照らし合わせて確認してね。

# ComputationalSecurity 引き継ぎメモ

## プロジェクト
/Users/yasube/Programming2/lean4/ComputationalSecurity
Lean 4 + Mathlib 4、確率はPMFを使用。

## 直前のセッションで完了したこと

### GuessingLemma.lean → コードフリーズ済み（変更禁止）

完成している宣言：
- `guessing_lemma_case1` : 仮定 `Pr[X 1;A;==1] - Pr[X 0;A;==1] > ε` → 結論 `Pr[b;Xb;A;a==b] > 1/2*(1+ε)`
- `flipA` : アルゴリズムの出力0↔1を反転する定義
- `Pr_flip` : `Pr[X;flipA A;==1] = 1 - Pr[X;A;==1]`
- `guessing_lemma_case2` : 仮定 `Pr[X 0;A;==1] - Pr[X 1;A;==1] > ε` → 結論 `∃ B, Pr[b;Xb;B;a==b] > 1/2*(1+ε)`（witness は `flipA A`）
- `guessing_lemma_abs` : 上記2ケースのOR → `∃ B, Pr[b;Xb;B;a==b] > 1/2*(1+ε)`

### 重要な数学的発見
- 推測補題の正しい形式化は「仮定のアルゴリズム A と結論のアルゴリズム B は同一でなくてよい」
- case2 では B = flipA A を witness とすることで証明できる
- この ∃ B の形にしたことで、テキストの「0と1の反転」操作が推測補題レベルで完全に吸収された
- テキスト補題3.1は仮定と結論で同じ A を使っているが、case2（符号が逆のケース）では厳密には別のアルゴリズムが必要であり、テキストの記述はこの点が曖昧

## 次のセッションで検討すること

テーマ：`guessing_lemma_abs` が `∃ B` を返すようになったことで、
テキスト定理3.1の証明（SemSec_Implies_Ind.lean）における
「攻撃者レベルでの反転操作」が自明に吸収されるかどうかの検討。

具体的には：
- `pa_gt_half` のシグネチャを確認
- `guessing_lemma_abs` の `∃ B` から得られる `B` を `B2` の構成にどう使うか
- `pa_gt_half` 自体も `∃` を含む形に変える必要があるか

